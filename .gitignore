# macOS / Linux
cat > .gitignore << 'EOF'
# dependencies
node_modules/
# env & secrets
.env
# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
# uploads / build artifacts
uploads/*
!uploads/.gitkeep*
!uploads/*
!uploads/.gitkeep.gitkeep
dist/
coverage/
.DS_Store
EOF
